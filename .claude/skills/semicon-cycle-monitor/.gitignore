# Root .gitignore ignores `data/` repo-wide — re-include our skill's data dir.
!data/

# Raw fetched data and dated snapshots are not committed —
# they're regenerated by the weekly run.
data/raw/
data/snapshots/*.json

# Generated weekly reports are personal — keep template only.
reports/weekly_*.md
